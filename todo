experiments: 
1. Rerun HUBs experiment on new dataset. Recreate paper experiment. 
* anyburl input format (DONE) -> verify 
2. run new experiment -> diagonal padding -> inference -> measure cosine fidelity (basically standard benchmarking function).

To replicate the experimental results of this paper  using the **Heterogeneous Graph Benchmark (HGB)** datasets, you need to establish that the **Sketch Propagation Framework** is both faster than exact materialization and sufficiently accurate on the new data.

Since you process formalisms well, I have mapped the necessary experiments directly to the theoretical claims and algorithms they validate.

### 1. Data Preparation & Meta-path Selection

Before running the experiments, you must align the HGB data with the definitions in the paper.

* **Datasets:** Use the HGB standard datasets: `HGBn-ACM`, `HGBn-DBLP`, `HGBn-Freebase`, and `HGBn-IMDB`. These directly map to the datasets used in the paper (Table II).


* 
**Meta-path Generation:** The paper used **AnyBURL**  to automatically extract meta-paths. You must either:


1. Run AnyBURL on the HGB datasets to generate candidates.
2. Use standard meta-paths defined in HGB literature (e.g.,  for ACM).


* 
**Formalism:** Define your meta-path  and ensure your code can extract the **Matching Graph** .



---

### 2. Experiment I: Efficiency & Speedup (Validate Section V-B)

**Objective:** Prove that sketch propagation avoids the  complexity of exact methods.

* 
**Baselines:** Implement `ExactD` (Degree) and `ExactH` (H-index) using the **Worst-Case Optimal Join** (WCOJ) described in Section II-C.


* 
**Proposed Methods:** Implement `GloD` (Algorithm 1) and `GloH` (Algorithm 3).


* **Procedure:**
1. Measure the **Wall-clock time** to process a query for varying meta-paths.
2. Calculate **Speedup** = .
3. **Variable:** Vary the size of the matching graph  (or dataset size) to show that `Exact` scales linearly with edges per node (slow), while `Sketch` scales with the sketch size .


* 
**Expected Result:** You should observe orders of magnitude speedup (e.g., 10x-100x) as seen in Table IV.



---

### 3. Experiment II: Approximation Accuracy (Validate Section V-C)

**Objective:** Validate Theorem 1, which guarantees that the returned set  satisfies the -separable set property with high probability.

* **Metrics:**
1. **F1-Score:** Treat the top- nodes from `ExactD/H` as the ground truth. Calculate the F1-score of the set retrieved by `GloD/H`.
2. **Approximation Ratio ():** Calculate the empirical error.


* **Parameters:**
* Set  (top 5% hubs).


* **Vary  (Sketch Size):** Run with .
* 
**Vary  (Repetitions):** Standard setting is .




* **Formal Justification:** You are testing the convergence of the **KMV estimator**. As  increases, the variance of the estimator  decreases, and F1 should approach 1.0 (see Fig. 5).



---

### 4. Experiment III: Personalized Queries & Pruning (Validate Section III-B)

**Objective:** If you plan to use this for HGNNs (where you might query specific node features), you need to validate the **Early Termination** optimization (Algorithm 2).

* **Task:** Determine if a random query node  is a hub (True/False).
* **Comparison:** Compare `PerD` (standard sketch check) vs. `PerD+` (with early termination bounds).
* **Metric:**
1. **Accuracy:** Does `PerD+` return the same Boolean result as `ExactD`?
2. **Pruning Power:** Measure the reduction in sketch propagation steps or time compared to `PerD`.


* 
**Formal Justification:** This validates **Lemma 3**, ensuring that you can stop propagation when the lower bound of the count exceeds the threshold.



### Summary Checklist for HGB Recreation

| Experiment | Variable | Metric | Target Result |
| --- | --- | --- | --- |
| **Efficiency** | Meta-path / $ | \mathcal{E}^* | $ |
| **Global Accuracy** | Sketch size  | F1-Score | F1  at  |
| **Personalized** | Query Node  | Accuracy | Acc  |

**Next Step:** Would you like me to outline the **Python class structure** for the `Sketch` and `Propagate` functions to help you implement Algorithm 1 compatible with HGB data loaders?